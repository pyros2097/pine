module main

enum Direction {
  Left
  Right
}

enum Position {
  Top
  Bottom
}

type User {
  name: string
  age: string
  gender: string
  address: string
  createdAt: string
  updatedAt: string
  deletedAt: string
}

proc adder(a: int, b: int) : int  {
  if a < b {
    return a - b + b
  }
  return a + b
}

test "for equal or not equal" {
  assert 1 == 1
  assert 2 == 2
  assert 4 == 4
  assert 3 != 13
}

