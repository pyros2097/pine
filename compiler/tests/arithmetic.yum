module main



type Int i32
type Float f32


enum Direction
  | Left
  | Right


type Person
  age: string
  name: string


// method fullName(p: Person): string =
//    "${p.name} ${p.name}"

proc addFloatRef(a: float, b: float): float =
  a + b

proc addIntRef(a: int, b: int): int =
  a + b

proc addInt(): int =
  5 + 3

proc subInt(): int =
  5 - 3

proc mulInt(): int =
  5 * 3

proc divInt(): int =
  5 / 3


