module main

enum Direction
  | Left
  | Right

enum Position
  | Top
  | Bottom

type User
  name: string
  age: string

// method fullName(p: Person): string =
//    "${p.name} ${p.name}"
proc addFloatRef(a: float, b: float): float =
  a + b

proc addIntRef(a: int, b: int): int =
  a + b

proc addInt(): int =
  5 + 3

proc subInt(): int =
  5 - 3

proc mulInt(): int =
  5 * 3

proc divInt(): int =
  5 / 3

test all() =
  addIntRef(1, 3)
  addInt()
  subInt()
  mulInt()
  divInt()
