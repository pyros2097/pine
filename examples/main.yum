module main

import "github.com/yum/std"
// some_stuff = a: num, b: num ->
//   a + b + 4
add = a: num, b: num -> num
  a + b + 4

another = a: num, b: num -> num
  add(a, b)

// useGlobal<T> = key: string, default: T -> T
// hacky stuff
// name, setName := useGlobal<string>("name", "123")
main = ->
  1 + 2
  3 + 4
  another(1, 5)

// "123 ${b} ${b}"